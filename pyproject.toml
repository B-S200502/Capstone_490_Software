[project]
name = "acoustic-imager"
version = "0.1.0"
description = "Acoustic imager capstone software"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "matplotlib>=3.7",
]

[tool.pyright]
reportMissingImports = "none"
